export interface email {
    email: string,
    id: string
}

export interface usertype {
    Cash: number | null,
    createdAt: string,
    email: string,
    isVerifyed: boolean,
    password: string,
    updatedAt: string,
    userid: string,
    username: string
}