export interface Earnings {
    id: string,
    amount: number,
    source: string,
    userid: string,
    createdAt: string,
    updatedAt: string
}

export interface Spendings {
    id: string,
    amount: number,
    spendOn: string,
    userid: string,
    createdAt: string,
    updatedAt: string
}